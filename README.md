# Student Profile App

## Description
This is a small Java console program built to act as a Student Profile App for a training centre[cite: 3, 24]. The application asks the user for some information and displays a summary[cite: 25]. 

It was built to practice foundational Java concepts, successfully fulfilling the following requirements:
* Storing data in variables using `String`, `int`, `double`, and `boolean`[cite: 5, 16].
* Taking user input with the `Scanner` class[cite: 6, 17].
* Using `if/else` conditional logic for decision making[cite: 7, 18].
* Implementing a simple loop[cite: 8, 19].
* Writing and calling at least one custom method[cite: 9, 20].

## Getting Started

### Prerequisites
* Java Development Kit (JDK) installed on your machine.

### How to Compile and Run
1. Open your terminal and navigate to the project directory.
2. Compile the Java file using the following command:
   ```bash
   javac ExerciseDayTwo.java